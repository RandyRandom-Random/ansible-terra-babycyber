---
- name: restart wazuh stack
  ansible.builtin.command:
    cmd: "{{ 'podman' if (runtime_engine | default('docker')) == 'podman' else 'docker' }} compose -f /opt/wazuh-docker/single-node/docker-compose.yml restart"
  changed_when: true
